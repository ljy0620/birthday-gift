# 部署后自测清单

部署完成后，按这个顺序检查：

## 0. 先跑自动体检（最重要）

```bash
node scripts/check-deploy.mjs https://你的网址.vercel.app
```

它会自动查四件事：站点能不能打开、倒计时是不是客户端实时计算、
**Supabase 环境变量有没有真的注入进去**、六个页面是否都可访问。

> 第 3 项是关键。环境变量漏填时，Vercel 照样构建成功、网页照样能打开，
> 但数据只会存在访问者自己的浏览器里。靠肉眼看是看不出来的，必须查。
> 这一项不过就别往下测了，先把环境变量补上再 Redeploy。

## 1. 首页
- 打开 Vercel 提供的网址
- 确认首页正常显示
- 确认倒计时秒数在跳（不动就是客户端脚本没加载）

## 2. 动态发布
- 打开"日常广场"
- 发布一条文字动态
- 刷新页面后仍然存在
- 换另一台电脑也能看到

## 3. 图片上传
- 选择一张本地图片
- 确认预览显示
- 发布动态
- 刷新后图片仍然显示
- 另一台电脑也能看到图片

## 4. 学习打卡
- 发布一条学习打卡
- 刷新后仍然存在
- 另一台电脑也能看到

## 5. 课程表和其他页面
- 打开课程表页、关于页、留言板页
- 确认都能正常访问

## 6. 跨设备同步（项目的核心目标）
- 在另一台电脑（或手机）打开同一个网址
- 在那边发一条动态
- 回到第一台电脑刷新，应该能看到那条动态

## 7. 如果出现问题
- **先跑第 0 步的脚本**，它会直接告诉你是哪一环坏了
- 环境变量问题 → Vercel → Settings → Environment Variables，改完必须 Redeploy
- 表或 Storage 问题 → 回本地跑 `node scripts/verify-supabase.mjs`

## 8. 大陆网络打不开怎么办
`*.vercel.app` 在国内经常连不上，这是域名问题不是部署问题。
判断方法：手机开热点（蜂窝网络）再打开一次，能开就是网络环境问题。
解决办法：绑自定义域名，或换国内托管（如腾讯 EdgeOne Pages）。
